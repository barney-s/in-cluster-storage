# Instance: ics-2 — deploy-kops-gce.md runbook instance
# The parameters that make this instance this instance.
# Sourced by deploy.sh and teardown.sh.

PROJECT="barni-cnrm-20260529"
REGION="us-central1"
ZONE="us-central1-a"
CLUSTER="ics2"
KOPS_CLUSTER_NAME="${CLUSTER}.k8s.local"
KOPS_STATE_STORE="gs://${PROJECT}-${CLUSTER}-kops-state"
GAR_LOCATION="us-central1"
GAR_REPOSITORY="in-cluster-storage"
IMAGE_TAG="latest"
REGISTRY="${GAR_LOCATION}-docker.pkg.dev/${PROJECT}/${GAR_REPOSITORY}"
